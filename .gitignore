# Gradle
.gradle/
build/
bin/
!gradle/wrapper/gradle-wrapper.jar

# Kotlin compiler daemon internal logs/state
.kotlin/

# Android / IDE
local.properties
*.iml
.idea/
captures/
.cxx/

# Secrets — never commit (CLAUDE.md rule 4)
*.keystore
*.jks
.env
providers.local.yaml

# Model weights (models/ keeps registry.json + checksums only)
models/*.gguf
models/*.onnx
models/*.bin
# Added by code-review-graph
.code-review-graph/
