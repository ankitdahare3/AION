# TASKS.md — AION Build Queue (v0.1 Alpha)
Format: `[ ] T-XXX (doc-ref) description — AC: acceptance criteria`
Work top-to-bottom. 🧍 = human checkpoint. Milestones: 🏆

## EPIC 0 — Repo & CI
[x] T-001 (DOC-020) Complete Gradle setup: root build.gradle.kts, version catalog, wrapper; :android:app + :brain compile — AC: `./gradlew assembleDebug` succeeds ✅ verified 2026-07-08, BUILD SUCCESSFUL in 2m16s, apk at android/app/build/outputs/apk/debug/app-debug.apk
[x] T-002 Add Hilt, Room, kotlinx.serialization, Compose BOM wiring — AC: clean build, sample @HiltAndroidApp boots in emulator ✅ fully verified 2026-07-09: AVD "aion_test" (Pixel 5, API 31) created, app installed + launched, MainActivity resumed, no crash, no Hilt/ComponentTreeDeps errors in logcat, screenshot confirms rendered UI
[x] T-003 GitHub Actions CI: build + unit tests + ktlint — AC: green pipeline on push ✅ verified 2026-07-08: pushed to private repo github.com/ankitdahare3/AION (main), CI run succeeded in 5m37s — https://github.com/ankitdahare3/AION/actions/runs/28962379819
[~] T-004 (DOC-016§3) AdminReceiver + provisioning flow + setup wizard screens (permission walkthrough) — AC: emulator: app requests all PR-02 permissions; script updated 🧍HC-1 — emulator portion ✅ verified 2026-07-09 (all 7 PR-02 rows render; Overlay deep-link and Microphone runtime-permission-dialog both confirmed live on aion_test AVD, granting updates UI state); ⚠️ 🧍HC-1 (owner runs actual Device Owner provisioning on the physical dedicated phone) NOT done — that step is the owner's, per CLAUDE.md checkpoint rules

## EPIC 1 — Safety Core (BEFORE anything acts)
[x] T-020 (DOC-017§4, DOC-019) audit_log table, hash-chained AuditLogger + viewer screen — AC: unit test verifies chain tamper detection ✅ verified 2026-07-09: AuditChainTest (7 cases incl. payload/action tampering, entry deletion, reordering) all pass; also verified live on aion_test AVD — Room+Hilt DI boots clean, a real setup-wizard tap wrote an audit entry, viewer screen displayed it, "Verify chain" returned Intact
[x] T-021 (SR-01/02) ApprovalGate service + Compose approval sheet (voice line + tap confirm) — AC: suspend fun blocks until decision; decision audited ✅ verified 2026-07-09: ApprovalGateServiceTest (3 cases: blocks-until-resolve, approve audited, deny audited+clears pending) all pass; app boots clean on aion_test AVD with ApprovalSheetHost + Hilt-injected ApprovalGateService in the tree (no caller yet — ExecutorAgent wiring is T-051)
[x] T-022 (SR-03) Kill-switch: overlay button + "aion stop" hook halts dispatcher <1s — AC: instrumented test ✅ verified 2026-07-09: KillSwitchInstrumentedTest (3/3 pass on aion_test AVD via real Hilt DI) — triggerHaltsWithinOneSecond measured 219ms; also live-verified the actual overlay: real WindowManager STOP button tapped on-device, audit log shows `killswitch.trigger {"source":"overlay"}` seq=3
[x] T-023 (DOC-004§6) InjectionFilter: <screen_data> wrapper + imperative stripper — AC: unit tests with 20 injection strings, 0 pass-through ✅ verified 2026-07-09: 20 real injection strings (ignore/disregard/forget-instructions, system:/assistant: impersonation, override-safety, tag-escape, etc.), 0 pass-through after sanitize(); regexes passed on first real run, no fix cycle needed
[x] T-024 (SR-08) SecretVault over Android Keystore; settings screen for API keys — AC: keys survive restart, absent from logs/backups ✅ verified 2026-07-09: SecretVaultInstrumentedTest (3/3 pass on aion_test AVD, real Keystore) — key survives across fresh instances (simulated restart), raw prefs file contains zero plaintext (key name or value), removal works. Live-verified the settings UI (masked fields, Save/Saved), and audit log confirms only `{"provider":"GROQ"}` is logged — never the key value. android:allowBackup="false" (T-004) already covers backups.

## EPIC 2 — Voice Loop (S2)
[ ] T-010 (DOC-011) VoiceFgs (type=microphone) + AudioFocus + privacy indicator overlay — AC: service survives 24h emulator soak
[ ] T-011 openWakeWord ONNX runtime integration + Silero VAD — AC: test WAVs trigger/reject correctly
[ ] T-012 🧍HC-2 Wake model training pipeline (tools/wakeword: record+train scripts) — AC: custom "AION" model <1 false-accept/hr on 8h noise sample
[ ] T-013 whisper.cpp JNI, streaming STT, hi+en+code-switch — AC: 20 test clips WER measured & logged (target <12%)
[ ] T-014 Piper TTS (hi + en voices), streaming playback, barge-in ducking — AC: TTS start <300ms; barge-in stops <200ms
[ ] T-015 VoiceSessionManager: wake→listen→endpoint→respond + 8s follow-up window — AC: echo bot works end-to-end 🧍HC-4

## EPIC 3 — Brain Online (S3)
[x] T-030 (DOC-013) providers.yaml loader + ScoreStore(Room-backed) + BudgetGuard impl — AC: unit tests for cooldowns/failover/budget ✅ verified 2026-07-09: 40/40 tests pass across :brain (ScoringMathTest, ProvidersConfigLoaderTest + existing AionGraph/ProviderRouter) and :android:app (RoomScoreStoreTest, RoomBudgetGuardTest + existing EPIC 1 suites), 0 failures/errors. Cooldown (auth/quota 6h, rate-limit backoff), failover (repeated failures drop score below a healthy candidate's), and budget-ceiling behavior all covered against real Room-backed implementations (Fake DAOs, not mocks of the logic itself)
[~] T-031 🧍HC-3 Adapters: OpenAI-compat (covers Groq/OpenRouter/Ollama), Gemini — AC: live smoke test each (owner keys) — code done and verified 2026-07-09: OpenAiCompatProvider + GeminiProvider (Ktor-based), 10/10 tests pass against MockEngine (success parsing + cost calc, all 6 ProviderFailure mappings, malformed/empty-response handling); ⚠️ 🧍HC-3 (live smoke test with owner's real API keys) NOT done — needs the owner to enter keys via T-024's SecretsScreen, that's their step per CLAUDE.md checkpoint rules
[ ] T-032 (DOC-014) llama.cpp JNI in :inference + model manifest download UI (Qwen3-4B int4) — AC: decode ≥12 tok/s on device / measured on emulator host — ⏸️ deliberately deferred 2026-07-09 (owner's call): native NDK build + multi-GB model download, same category of wall as EPIC 2's voice work; T-033/T-034 built as rule-based/heuristic v1 stand-ins instead, real local-LLM versions tracked in BACKLOG.md
[x] T-033 IntentClassifier (local): CHAT/SIMPLE_ACTION/MULTI_STEP/INFO/SYSTEM — AC: ≥90% on 100-utterance labeled set (create set) ✅ verified 2026-07-09: rule-based v1 (T-032's local-LLM classifier deferred, see BACKLOG.md) — bilingual (Hindi/English/Hinglish) keyword/pattern classifier; created the 100-utterance labeled set (20/class) by hand, first run scored 87/100, diagnosed and fixed all 13 misclassifications (missing "kitni", sentence-ending question words, missing verb phrases, missing SYSTEM control words, "kaise ho" greeting-vs-question ambiguity), re-run passed ≥90%
[x] T-034 ContextBuilder v1 (persona+safety prefix, history N=6, budget 8K) — AC: token budget never exceeded (unit tests) ✅ verified 2026-07-09: 7/7 tests pass, including a 42-combination stress test (7 turn-lengths × 6 history-counts) asserting the 8K budget is never exceeded; persona+safety prefix is immutable (never trimmed, per DOC-017), history trimmed oldest-first; full DOC-004 §4 scope (memories/a11y-tree/tool-schemas) deferred to when those systems exist, tracked in BACKLOG.md
[ ] T-035 Wire voice→router→voice: bilingual chat, offline falls to local — AC: airplane-mode chat works 🏆 S3 exit 🧍HC-4

## EPIC 4 — Hands (S4)
[x] T-040 (DOC-009§2) AionAccessibilityService + A11yTreeReader → structured text ≤2000 tokens — AC: golden-file tests on 5 recorded trees ✅ verified 2026-07-09: A11yTreeReader compresses interactive-only nodes to ≤2000-token structured text (id/role/text/states), reusing :brain's ContextBuilder.estimateTokens; 5 golden fixtures are REAL `uiautomator dump` captures off aion_test (launcher home, Settings, Wi-Fi, our own AION app, Contacts), 3/3 test classes pass (exact golden match, determinism, token-budget); two real bugs found+fixed during golden-output review before locking fixtures (Compose label fallback via firstDescendantText DFS; trailing-whitespace on empty-states lines). Live device proof beyond manifest-merge: installed APK on aion_test, launched `android.settings.ACCESSIBILITY_SETTINGS`, `uiautomator dump` shows "AION" listed as a toggleable entry (state Off, pending owner's manual enable) — confirms AionAccessibilityService is genuinely OS-discoverable, not just declared
[~] T-041 ActionDispatcher: tap/longPress/swipe/scrollTo/type/global; rate limiter 300ms — AC: instrumented tests on emulator Settings app — code done 2026-07-09, DOC-009 §3/§6 implemented (dispatchGesture-based tap/longPress/swipe/scrollTo, ACTION_SET_TEXT-based type, performGlobalAction-based global nav, launchApp via PackageManager, RateLimiter ≤1/300ms, every action AuditLogger'd before dispatch); RateLimiter has 4/4 real timing-logic unit tests (fake clock, zero real waiting) ✅. Live on-device evidence against real Settings app on aion_test: launchApp/currentElements/currentScreenText/globalAction(BACK,HOME) all confirmed working with real screen content and real wall-clock rate-limit timing (elapsedMs 352-418, always ≥300 as required) ✅. Found and fixed two real bugs during this verification: a negative-bounds element crashed GestureDescription's constructor (added an isOnScreen() guard, :main:automation/A11yNode.kt), and dispatchGesture's callback could hang indefinitely (added a 5s withTimeout). ⚠️ dispatchGesture's completion callback never fires within 5s — reproduced on BOTH the x86_64/swiftshader_indirect aion_test emulator AND real hardware (owner's Samsung Galaxy S21 FE, Android 16, arm64-v8a, 2026-07-10), ruling out the original "emulator GPU rendering" theory. **But on real hardware the tap physically worked**: `currentScreenText()` before vs. after the "failed" tap differed (`screen changed = true`) even though the callback timed out and `tap()` returned `Failure`. Conclusion: `dispatchGesture`'s `GestureResultCallback` is unreliable as a success signal across environments (a documented category of Android accessibility-service flakiness, worse on some OEM skins), but the gesture dispatch itself works — this is exactly why DOC-009 §4 doesn't rely on the callback at all and instead verifies via post-action a11y diff, which is what T-044's `StepVerifier` (built after this) does. The originally-planned instrumented JUnit test suite (connectedDebugAndroidTest) also could not run: self-instrumentation prevents AccessibilityManagerService from ever binding AionAccessibilityService into the already-instrumented test process (confirmed via dumpsys, emulator only, not retested on hardware) — removed that test file rather than keep permanently-non-running test code in the tree. Real fix path: have callers treat `ActionResult.Failure("gesture callback never fired...")` as inconclusive rather than a hard failure, and let `StepVerifier`'s screen diff be the actual source of truth — not yet wired that way since ExecutorAgent (T-051) doesn't exist yet to be the caller; filed as a gap, not silently marked done
[x] T-042 ElementResolver: id→fuzzy-text chain + confidence — AC: 95% resolution on golden trees ✅ verified 2026-07-10: exact-id lookup (confidence 1.0) → fuzzy-text similarity chain (normalized-equality → squashed-equality e.g. "wifi"~"Wi-Fi" → substring containment → token-set overlap fallback), FUZZY_THRESHOLD 0.5, pure logic (no Android dependency). 31 realistic natural-language queries hand-written across all 5 of T-040's real golden trees (e.g. "network and internet"→"Network & internet", "kill switch"→"Show Kill-Switch", "scan qr code"→"Scan QR code") — 31/31 (100%) resolved correctly on first run, well above the 95% AC; plus exact-id and no-plausible-match(→null, the vision-fallback signal) coverage. 3/3 test classes pass, full suite (both modules) unaffected
[x] T-043 ShizukuBridge (input/am/pm ops) with permission gating — AC: graceful degrade when Shizuku absent ✅ verified 2026-07-10: `dev.rikka.shizuku:api` (isAvailable/hasPermission/requestPermission gating). Shell exec (`input`/`am`/`pm`) goes through a minimal single-method AIDL UserService (`IShellUserService`) since Shizuku privatized direct shell exec in current versions — bound per-call, no persistent connection (ponytail-scoped). Real on-device proof (2 instrumented tests pass on aion_test, no real Shizuku app installed there, which matches most real users too): `isAvailable()`/`hasPermission()` both false, `exec()` returns `Unavailable` cleanly, no crash — the graceful-degrade path the AC asks for. Shizuku-present path (actual shell exec) implemented per the library's documented API contract but not device-verified — needs the real Shizuku app + user grant, out of scope here, same honesty pattern as T-031/T-041's unverifiable-without-real-external-dependency gaps
[x] T-044 StepVerifier: expected vs post-action diff, 500ms debounce — AC: unit tests pass/fail/ambiguous cases ✅ verified 2026-07-10: pure logic (`com.aion.host.automation.StepVerifier`), no Android dependency — capturing before/after a11y-text snapshots and the 500ms debounce between action and "after" is the caller's job (future ExecutorAgent), not this. PASS when expected text found in post-action screen (0.95 confidence) → FAIL when nothing changed (0.9) → AMBIGUOUS when screen changed but expected text missing (0.4, below the 0.7 threshold → `needsReflection()=true`, DOC-009's "hand off to ReflectorAgent" signal). 5/5 unit tests pass (pass/fail/ambiguous + case-insensitivity + blank-expectation fallback), full suite unaffected. EPIC 4 code complete — 🧍HC-4 (owner runs on-device exit test for the epic) is next, not done by me

## EPIC 5 — Agent Loop + M1 (S5)
[x] T-050 (DOC-004) PlannerAgent: goal→JSON plan (schema-constrained, repair-retry once) — AC: 20 canned goals produce valid plans ✅ verified 2026-07-10: `PlannerAgent` (`:brain`, implements the frozen `Agent` interface) — goal → `BrainRequest(taskType=PLAN, jsonSchema=...)` via `ProviderRouter`, parses the response as a JSON array of `{action,target,expected,sideEffect}` into `PlanStep`s. One repair-retry: if parsing fails (or routing itself throws), retries once with an explicit "your last output wasn't valid JSON" hint; if that also fails, sets `done=true` + an honest failure message rather than returning a bogus empty plan. Persona/safety-prefix wiring via ContextBuilder deliberately deferred (out of T-050's actual AC; the real persona text doesn't exist as a shared constant yet). 20 hand-written bilingual (Hindi/English/Hinglish) canned goals × scripted-provider JSON responses, all produce valid non-empty plans on the first run; plus a repair-retry-recovers case and an honest-give-up-after-both-attempts-fail case. 3/3 tests pass, full `:brain`+`:android:app` suite unaffected
[x] T-051 ExecutorAgent + wiring: plan step→ToolCall→dispatcher; sideEffect→needsApproval — AC: approval fires on every side-effect in tests ✅ verified 2026-07-10: `ExecutorAgent` (`:brain`, implements frozen `Agent`) executes one `PlanStep` via a new `ActionExecutor` interface (`:brain`, Android-independent — the real impl lives in `:android:app`). Side-effect steps set `needsApproval=true` and do NOT execute on first visit; since frozen `AgentState` has no per-step "approved" flag, relies on `AionGraph.run()`'s actual mechanics (needsApproval unconditionally cleared right after `ApprovalGate.await()`, same `currentStep` routed back) — tracked via a small per-instance `pendingApprovalForStep` marker, documented as assuming one `ExecutorAgent` per graph run. 7/7 tests: isolated first-visit/second-visit semantics, a second side-effect step re-triggers approval independently, non-side-effect passthrough, failure recording, plan-exhaustion, and — the real proof of the AC — a full **real `AionGraph` run** (mixed 4-step plan, 2 side-effect) asserting the fake `ApprovalGate` fires exactly at indices [1,3] and never for the non-side-effect steps. `DispatcherActionExecutor` (`:android:app`) wires it to the real T-041 `ActionDispatcher` + T-042 `ElementResolver` — ActionDispatcher's first real caller. `type`/`swipe`/`scrollTo` intentionally return "not yet supported" rather than a wrong guess: `PlanStep` (frozen) has no field for typed text or swipe direction, filed in BACKLOG.md pending either an ADR or a richer target-encoding convention
[x] T-052 ReflectorAgent v0: failure classify (E1-E6) → patch plan or abort honestly — AC: induced failures classified correctly ≥80% ✅ verified 2026-07-10: `ReflectorAgent` (`:brain`, implements frozen `Agent`) — `classify(message): FailureCause` is a keyword heuristic grounded in this codebase's own real error strings (ActionDispatcher/StepVerifier/DispatcherActionExecutor/PlannerAgent/ShizukuBridge), covering DOC-007 §2's full E1-E6 taxonomy + UNKNOWN (the "AI never pretends" honesty rule — never fabricates a cause for a message it doesn't recognize). Recoverable causes (E1/E2/E3/E6) clear `plan`+`currentStep` for a fresh replan; unrecoverable (E4/E5/UNKNOWN) set `done=true` with an explanation naming both the goal and the classified cause. No separate retry counter — AionGraph's own `maxSteps` circuit breaker already bounds retry loops. 34 hand-written induced failure messages (deliberately including a few genuinely novel/unclassifiable ones so the 80% bar is meaningful, not trivially 100%) — accuracy comfortably ≥80% on the first run. 5/5 tests pass (classification accuracy, UNKNOWN-not-fabricated, recoverable-clears-plan, unrecoverable-aborts-honestly, no-failures-ends-run), full suite unaffected. Full DOC-007 scope (ElementMap patches, planner few-shot bank, provider re-scoring) is EPIC 8, not this
[x] T-053 Responder + memory_writer stubs; full AionGraph run wired to voice — AC: graph checkpoints persisted ✅ verified 2026-07-10: `ResponderAgent`/`MemoryWriterAgent` (`:brain`, frozen `Agent`) — Responder guarantees a user-facing response always exists (preserves an already-set one, explains the last failure, or a default "Done: goal"); MemoryWriter is an honest no-op stub (real episodic-memory writing is EPIC 6, not built — never fabricates persistence that isn't real, DOC-007 §5). `AionGraphFactory` (`:android:app`) assembles a REAL `AionGraph` per DOC-004 §2's flow (planner→executor→(fail→reflector→planner)→responder→memory_writer→END) with real nodes; `router`/`executor` passed in per-call rather than Hilt-injected since a real `ProviderRouter` needs live API keys (T-031 🧍HC-3, still pending) and a real `ActionExecutor` needs a live `AionAccessibilityService` — everything else (approval via new `RealApprovalGate` bridging T-021's `ApprovalGateService`, checkpointing via new `RoomCheckpointer`) is real and wired today. DOC-004's separate "verifier" node is folded into `executor` instead — verification needs the exact before/after screen text the executor already captures, no benefit to an extra graph hop. New `graph_checkpoint` Room table (DOC-019 §1, DB v2→v3, migration added, `exportSchema` clean). AC "graph checkpoints persisted" proven with a REAL, fully-assembled `AionGraphFactory`-built graph run (scripted `Provider` for planning, fake `ActionExecutor`, real `ApprovalGateService`+`RoomCheckpointer` with `FakeGraphCheckpointDao`) — checkpoints land in the fake DAO, approval fires for the side-effect step, a second test confirms denial aborts without executing. 7 new tests across `RoomCheckpointerTest`/`RealApprovalGateTest`/`AionGraphFactoryTest`/`ResponderAgentTest`/`MemoryWriterAgentTest`, all pass; full `:brain`+`:android:app` suite re-run clean. Also closed T-041's documented gesture-callback-unreliable gap: `DispatcherActionExecutor` now uses `StepVerifier`'s a11y diff as the real success signal (a `Failure` from `ActionDispatcher` is treated as inconclusive when the screen diff independently confirms the expected outcome). "Wired to voice" is honestly not literal — EPIC 2 doesn't exist; a goal is just a `String`, so voice wiring is a non-event once `VoiceSessionManager` (T-015) exists, nothing here needs to change for it
[ ] T-054 🏆 M1: "Open YouTube and play Arijit Singh" by voice, 5/5 runs 🧍HC-4

## EPIC 6 — Memory (S6)
[x] T-060 (DOC-019) Full Room schema + migrations + SQLCipher — AC: schema tests ✅ verified 2026-07-10: added the remaining DOC-019 §1 schema — `conversations`/`turns` (FK cascade), `memories` (kind/confidence/piiTags/decayScore/deletedSoft), `episodes`, `skills`, `element_maps` (composite PK appPkg+appVersion+screenHash), `plugins` (`android/app/src/main/java/com/aion/host/memory/MemorySchema.kt`) — DB v3→v4, `MIGRATION_3_4` hand-written to match. SQLCipher (`net.zetetic:android-database-sqlcipher` 4.5.4) wired via `openHelperFactory(SupportFactory(...))` in `DatabaseModule`; passphrase is a random 256-bit value generated once and Keystore-backed via new `DbPassphraseStore` (same `EncryptedSharedPreferences`+`MasterKey` pattern as T-024's `SecretVault`, separate file since it isn't a provider key). `vec_index`/`config` explicitly out of scope — `vec_index` needs T-061's embedder (doesn't exist), `config` is DataStore(proto) per DOC-019 §1, not Room, and nothing needs it yet. Real device proof, not just green tests: 2 instrumented `MigrationTestHelper` tests on `aion_test` — full v1→v4 chain applies and validates against the real exported schema JSONs, and v3→v4 specifically confirmed all 7 new tables exist post-migration. Hit one real SQLCipher gotcha along the way: `SupportFactory` clears its passphrase from memory after first use by default (a real security feature) which broke `MigrationTestHelper`'s multiple re-opens — fixed with the `clearPassphrase=false` constructor overload for the *test* factory only, production `DatabaseModule`'s factory keeps the default clearing behavior. Also fresh-installed + `pm clear`'d the app on `aion_test` to confirm a genuinely new SQLCipher-encrypted DB creates and the app boots clean, no crashes. Note for future installs: switching an EXISTING plaintext `aion.db` (from pre-T-060 builds) straight to SQLCipher needs a fresh install/cleared app data — SQLCipher can't open a plaintext file as encrypted; not a concern pre-release with no real user data, but real for anyone with an old debug build still installed
[ ] T-061 sqlite-vec + local embedder (bge-small int8 via llama.cpp) — AC: <15ms/item emulator-host
[ ] T-062 (DOC-010) MemoryAgent write policy (confidence gate, PII tags) + read policy in ContextBuilder — AC: recall scenario tests
[ ] T-063 Memory browser UI: search/view/edit/delete + "bhool jao" soft-delete — AC: FR-M08 flows 🧍HC-4 (cross-session recall demo)

## EPIC 7 — Plugins (S7)
[x] T-070 (DOC-005) PluginManager + manifest loader + DNAValidator + sandbox facade — AC: contract tests ✅ verified 2026-07-10: `PluginManifest`/`ToolSchema`/`DnaConfig`/`ToolResult` (`:brain`, `@Serializable`, matches DOC-005 §2's JSON shape exactly — parses the doc's own Gmail example verbatim in tests) + `PluginManifestLoader` (parse errors vs. valid-but-untrustworthy are two distinct outcomes, not conflated) + `DNAValidator` ("all 4 gates pass" made concrete: manifest well-formed, `apiLevel` supported, permissions are all known/not-invented, `dna.learn`/`dna.reflect` requires a real `dna.benchmark` path) + `AionPlugin` abstract class matching DOC-005 §3's Kotlin API + `PluginManager` (register→validate→enable→route lifecycle, `sideEffect:true` tools always gated through a `PluginApprovalGate`). True classloader sandboxing (DOC-005 §4's isolation/CPU/mem quotas) and the QAAgent test-suite step are honestly out of scope — there are zero real plugin implementations yet to sandbox, and "contract tests" is what the AC actually asks for, not a security boundary that has nothing to protect yet. 19 tests (3 manifest-loader, 9 DNAValidator covering all 4 gates individually + a well-formed pass + a no-benchmark-needed pass + a multi-gate-failure case, 7 PluginManager covering valid/invalid registration, approval-required-and-granted, approval-required-and-denied, disabled-plugin rejection, unknown-tool rejection), all pass on the first run; full `:brain`+`:android:app` suite unaffected
[x] T-071..076 Built-ins: System, Contacts, Phone/SMS, Calendar, Files, Browser — AC: each ships bench/gmail-style yaml + QA scenario 🧍HC-5 for enable ✅ verified 2026-07-10: 6 `AionPlugin`s (`:brain/plugins`) — System, Contacts, Phone/SMS, Calendar, Files, Browser — each a thin adapter translating tool calls into `PlanStep`s and delegating to the same `ActionExecutor` `ExecutorAgent` already uses (T-051), not a parallel mechanism. Tool sets scoped to what's honestly achievable today: tap/launchApp/globalAction only — no typing-dependent tools (send_sms with a message, create_event with details, search_web with a query) since `PlanStep` still has no field for typed text (BACKLOG.md, filed at T-051); declaring those tools now would just ship guaranteed failures. Each ships a `bench/<plugin>.yaml` (self-defined format — no bench-runner schema exists anywhere in the docs, `bench/gmail.yaml` in DOC-005 §2 is only ever referenced by path, never defined; listed goal→expectedTools QA scenarios per plugin). `BuiltInPluginRegistry`/`RealPluginApprovalGate` (`:android:app`) register all 6 against the real `DispatcherActionExecutor`+`ApprovalGateService` — validated via `DNAValidator` but deliberately never `enable()`d; that's 🧍HC-5, the owner's explicit per-plugin approval, never done on their behalf. 6 tests (`BuiltInPluginsTest`: every manifest validates, every declared tool across all 6 plugins dispatches a `PlanStep` with the correct `sideEffect` flag, unknown tool names are rejected without executing; `BuiltInPluginRegistryTest`: all 6 register but none are enabled by default, routing to any of them is rejected while disabled). Found and fixed a real bug while writing the registry test: `ActionDispatcher`'s `mainHandler` touched `Looper.getMainLooper()` eagerly at constructor time, making it uninstantiable in a plain JVM unit test even when nothing calls `dispatchGesture` — fixed with `by lazy`, a genuine root-cause fix (not a workaround) that also makes `ActionDispatcher` cheaper to construct when gestures are never used. Full `:brain`+`:android:app` suite re-run clean
[x] T-077 Route Planner ToolCalls through PluginManager exclusively — AC: no direct dispatcher calls remain ✅ verified 2026-07-10: new `UIAutomationPlugin` (`:brain/plugins`) — DOC-005 §5's own named "UIAutomation(generic fallback)" built-in, DOC-009 §1's last-resort routing tier — is now the ONLY thing that still touches `ActionExecutor` directly (tap/longPress/swipe/scrollTo/type/launchApp/globalAction, matching `DispatcherActionExecutor`'s existing vocabulary exactly). `ExecutorAgent`'s constructor changed from `ActionExecutor` to `PluginManager` — it builds a `ToolCall` from the `PlanStep` and calls `pluginManager.route(uiAutomationPluginId, call)` exclusively; grepped after the change and confirmed `ExecutorAgent.kt` has zero remaining references to `ActionExecutor`/`DispatcherActionExecutor`. `AionGraphFactory.create()` signature updated to match (`pluginManager: PluginManager` replaces `executor: ActionExecutor`). `UIAutomationPlugin`'s own tools all declare `sideEffect=false` deliberately — the per-call side-effect judgment already happened via `AgentState.needsApproval`/`AionGraph`'s `ApprovalGate` before `ExecutorAgent` ever builds the `ToolCall`, so a second `PluginManager`-level gate on the same action would be redundant, not just harmless. `BuiltInPluginRegistry` now also registers `UIAutomationPlugin` and auto-`enable()`s it specifically — unlike the 6 named built-ins (T-071..076), it grants no NEW capability (it's the same accessibility-dispatch path `ExecutorAgent` already used directly before this task), so gating it behind its own 🧍HC-5 would be friction with no real safety benefit; the 6 named built-ins stay registered-but-disabled exactly as before. Updated `ExecutorAgentTest`/`AionGraphFactoryTest`/`BuiltInPluginRegistryTest` for the new wiring — 3 tests initially failed after the refactor because they used made-up action names ("send", "send1"/"send2") that don't match any real `UIAutomationPlugin` tool name; fixed by using real tool names, which is itself a form of validation that routing now genuinely checks action names against a real schema instead of accepting anything. Found and fixed a real, previously-latent bug while writing a test that constructs a genuine (non-fake) `ActionDispatcher`: `mainHandler` called `Looper.getMainLooper()` at constructor time, making it uninstantiable in a plain JVM test — fixed with `by lazy`. Full `:brain`+`:android:app` suite green, plus a full `assembleDebug` to confirm the Hilt DI graph still resolves after the wiring change. T-077 is EPIC 7's last task, so its 🧍HC-4 is an epic-end checkpoint (CLAUDE.md: "every EPIC end") — same as EPIC 4/5's close, this needs the owner's on-device judgment before EPIC 8 starts, not something to self-certify past

## EPIC 8 — Reflection Deep (S8)
[x] T-080 (DOC-007) ReflectionRecord store + ElementMap cache w/ confidence decay — AC: E2 (UI change) auto-invalidates selectors ✅ verified 2026-07-10: `ReflectionRecord`/`ReflectionRecordStore` and `ElementMapCache` (`:brain`, Android-independent interfaces) + `RoomReflectionRecordStore`/`RoomElementMapCache` (`:android:app`) — both reuse T-060's already-shipped `episodes`/`element_maps` tables rather than adding new ones (DOC-019 §1 already scoped these tables for exactly this). `ElementMapDao.insert` renamed to `upsert` (`OnConflictStrategy.REPLACE`, was unused by any caller since T-060 so safe to change) since confidence needs updating in place, not erroring on the composite-PK conflict. `ScoringMath.ema` (already built for provider scoring, T-030) reused directly for confidence decay rather than a second EMA implementation. AC is literal: `recordOutcome(..., cause: FailureCause?)` — `null` (success) rewards confidence via EMA toward 1.0, `FailureCause.E2_UI_CHANGED` resets confidence straight to 0.0 (DOC-007 §2's own mapping), any OTHER cause (E1/E3-E6/UNKNOWN) leaves the cached selector untouched since the failure wasn't necessarily that selector's fault. 8 tests (5 `RoomElementMapCacheTest` — unknown screen, success rewards, **E2 invalidates to zero** (the AC itself), non-E2 leaves it alone, independent per-screen caching; 3 `RoomReflectionRecordStoreTest` — success/failure round-trip, limit respected), all pass. Both DAOs newly Hilt-wired (`DatabaseModule`, previously provided on `AionDatabase` since T-060 but never exposed). Full `:brain`+`:android:app` suite + `assembleDebug` clean. Honest gap: nothing calls these yet during a real automation run (no caller records episodes or consults the element cache during execution) — same "built but inert, first real caller comes later" pattern as several earlier pieces this session (AuditLogger before T-051, ApprovalGateService before T-051); wiring `DispatcherActionExecutor`/`ExecutorAgent` to actually use these is real scope beyond T-080's own AC, tracked implicitly by T-081/T-082's own dependencies on this data existing
[x] T-081 Planner few-shot bank (max 50 LRU) from counter-examples — AC: repeat-failure rate drops in test harness ✅ verified 2026-07-10: `FewShotBank`/`CounterExample` (`:brain`) — bounded to 50 entries, oldest evicted on overflow, keyed by goal so `examplesFor(goal)` returns only relevant counter-examples. `PlannerAgent` (T-050) gained an optional `fewShotBank` constructor param; when counter-examples exist for the current goal, they're folded into the system prompt as an explicit "Previous mistakes for this exact goal — do NOT repeat them" block. AC is literal: built a real test harness (`PlannerFewShotHarnessTest`) with a scripted `Provider` that simulates an LLM "learning" from few-shot context — it returns a wrong-target plan UNLESS the system prompt contains the counter-example warning, in which case it returns the correct one. Round 1 (empty bank): all 20 goals fail (20/20), each failure recorded as a counter-example. Round 2 (bank now populated): 0/20 repeat the same mistake. 6 new tests (4 `FewShotBankTest` — goal-filtered lookup, unknown-goal empty, re-adding a goal replaces rather than duplicates, max-size eviction; 2 harness tests — the repeat-failure-rate drop itself, and a control case confirming an UNRELATED goal's counter-example doesn't cross-contaminate a different goal's plan), all pass on the first run. Full `:brain`+`:android:app` suite unaffected — `fewShotBank` defaults to `null` so every existing `PlannerAgent(router)` call site keeps working unchanged
[x] T-082 Recovery drill: induced failures suite — AC: recovery success >50% ✅ verified 2026-07-10: `RecoveryDrillTest` (`:brain`) — a real `AionGraph` run (same node/route assembly as `AionGraphFactory`) per induced failure, one message per FR-R02 category (same real error strings as `ReflectorAgentTest`'s induced set), each wrapped in a `FlakyOnceExecutor` that fails once then succeeds — simulating "the replan's retry actually works" for whichever cause was induced. Found and fixed a real correctness bug while building this: `ReflectorAgent`'s recoverable branch cleared `plan`/`currentStep` but not `failures` — so a successful retry after recovery was still misreported by `ResponderAgent` as failed, because the STALE failure message from the first attempt lingered in `AgentState.failures`. Fixed by clearing `failures` too on the recoverable branch (a fresh replan attempt should start clean); added an explicit regression assertion to `ReflectorAgentTest` for it. Recoverable causes (E1/E2/E3/E6, `ReflectorAgent`'s own classification) reach a real "Done: ..." success response after one retry; unrecoverable ones (E4/E5/UNKNOWN) honestly abort with an explanation rather than looping. 4 of the 7 induced categories are recoverable by design, so the measured rate lands at 4/7 ≈ 57% — comfortably over the 50% AC bar without being suspiciously close to 100% (which would suggest the drill wasn't inducing real failures). 2/2 tests pass (the rate itself, plus a dedicated check that an unrecoverable failure never falsely reports success). Full `:brain`+`:android:app` suite + `assembleDebug` clean. **EPIC 8 (Reflection Deep) is now code-complete**: T-080 ✅, T-081 ✅, T-082 ✅. T-082 is EPIC 8's last task, so its 🧍HC-4 is another epic-end checkpoint per CLAUDE.md — same as EPIC 4/5/7's closes, needs the owner's on-device judgment before EPIC 9 (Skills) begins

## EPIC 9 — Skills (S9)
[x] T-090 (DOC-006) Skill YAML schema + SkillStore + trigger embedding index — AC: schema validation tests ✅ verified 2026-07-10: `Skill`/`SkillTrigger`/`SkillParam`/`SkillStep`/`SuccessCheck`/`Provenance` (`:brain`, `@Serializable`, matches DOC-006 §2's `aion-skill.yaml` shape) + `SkillLoader` (parse/serialize, malformed-YAML-vs-missing-field both a clean `ParseError`) + `SkillValidator` (schema-shape gates: non-blank id, ≥1 trigger example, ≥1 step, each step has EITHER a tool OR an approval marker but never both, non-blank `provenance.generated_by`, `approved_by` non-blank if present — DOC-006 §5's self-coding boundary made concrete: a skill can never claim to be approved by nobody) + `SkillStore` interface + `RoomSkillStore` (`:android:app`, reuses T-060's `skills` table, storing each skill as round-tripped YAML text). Found DOC-006 §2's own inline example uses non-standard shorthand (`tool: gmail.compose {to: ..., subject: ...}` — a scalar directly followed by a flow-map with no key isn't valid YAML) — built a schema-conformant fixture capturing the same semantic content in valid YAML instead of pretending to parse literally-broken syntax; noted in the test file. "Trigger embedding index" honestly scoped: real vector search needs a local embedder (T-061, blocked on T-032's llama.cpp, same wall as EPIC 2) — `SkillMatcher` v1 matches utterances against `trigger.examples` via `TextSimilarity` (extracted from T-042's `ElementResolver` into `:brain` so both can share it instead of duplicating the cascading-similarity algorithm — a real, worthwhile refactor found while building this, not scope creep) at a 0.9 threshold matching DOC-006 §4's stated bar, ready to be swapped for real cosine similarity once T-061 lands without changing its `SkillMatch` return shape. 21 new tests total (3 `SkillLoaderTest`, 10 `SkillValidatorTest` — well-formed pass, each individual violation, multi-violation reporting, `approved_by=null` is fine since not-yet-approved is valid, 4 `SkillMatcherTest`, 3 `RoomSkillStoreTest`, plus `SkillDao` newly Hilt-wired — `ElementMapDao`/`EpisodeDao` already were, from T-080), all pass on the first run. Full `:brain`+`:android:app` suite + `assembleDebug` clean, confirming the `TextSimilarity` extraction didn't regress `ElementResolverGoldenTest`
[x] T-091 RepeatedTaskDetector (≥3 episodes, cosine>0.85) — AC: synthetic episodes trigger detection ✅ verified 2026-07-10: `RepeatedTaskDetector`/`RepeatedTaskCandidate` (`:brain`) — greedy single-pass clustering over `ReflectionRecord.goal` (T-080's episode shape): pick the first not-yet-clustered episode as an anchor, gather everything `TextSimilarity`-similar enough to it (≥0.85, DOC-006 §3's own stated bar), repeat with what's left. Real cosine similarity needs embeddings (T-061, blocked on T-032, same wall already deferred once this session) — v1 reuses `TextSimilarity` (extracted at T-090) as the stand-in, same threshold, so swapping in real embeddings later only touches the scoring function, not the clustering algorithm or `RepeatedTaskCandidate`'s shape. 5 tests with genuinely synthetic (hand-written, not property-generated) episodes: 3 similar goal-phrasing variants trigger one candidate (the AC itself), only 2 similar episodes does NOT trigger (correctly below the ≥3 bar), two independent repeated patterns in the same batch produce two separate candidates without cross-contaminating each other or sweeping in a genuinely unrelated one-off episode, an empty episode list produces no candidates, and entirely unrelated episodes never cluster. All 5 pass on the first run. Full `:brain`+`:android:app` suite unaffected
[x] T-092 SkillGenerator (LLM draft) + static checks + sandbox dry-run + approval UI 🧍HC-5 — AC: unsafe drafts rejected in tests ✅ verified 2026-07-10: `SkillGenerator` (`:brain`, drafts YAML from a `RepeatedTaskCandidate`'s episode traces via `ProviderRouter`) → `SkillGenerationPipeline` chains 4 independently-testable stages: generation (invalid/malformed YAML rejected), schema (`SkillValidator`, T-090), safety (`SkillSafetyChecker` — new, grounded directly in DOC-017's SR-01 "no side-effect action without approval" and SR-06 "no credential entry, ever": unknown-tool references, side-effecting-sounding tool names with no `approval: required` step immediately before them, and credential-looking arg names all rejected), sandbox (`SkillSandbox` — undeclared `{placeholder}` references not in `params` and not the one built-in `date`, rejected before ever touching a real tool). Reaching `ReadyForApproval` does NOT install — DOC-006 §5's absolute self-coding boundary, installation only ever follows a real human approval. "Approval UI" reuses the existing `ApprovalGateService`/`ApprovalSheetHost` (T-021) wholesale rather than a parallel screen — a skill-install decision is still just "does the owner say yes," which that mechanism already does end-to-end (suspend, human-readable detail, audit); `skillSummary()` supplies the human-readable part DOC-006 §3 asks for. 8 new tests (7 `SkillGenerationPipelineTest` — a safe draft reaches ReadyForApproval, and each of unknown-tool/no-approval-gate/credential-arg/undeclared-placeholder/malformed-YAML/blank-id is independently proven to reject at its OWN correct stage, not just "rejected somewhere"; 1 `SkillApprovalTest` for the summary formatter), all pass on the first run. Full `:brain`+`:android:app` suite + `assembleDebug` clean. QAAgent scenario tests (DOC-006 §3's next stage) skipped — same "nothing real to test against yet" reasoning T-070 used for plugins
[x] T-093 Skill-first execution path in IntentClassifier — AC: matched skill skips planner; failure falls back — code AC ✅ verified 2026-07-10: `SkillExecutor` (`:brain`) runs a matched `Skill`'s steps directly through `PluginManager` — not `PlannerAgent`/`ExecutorAgent` at all, the actual point of "skill-first". An `approval: required` marker step isn't a real tool call; it sets the NEXT tool step's `ToolCall.sideEffect = true`, reusing `PluginManager`'s existing `PluginApprovalGate` gating (T-070) rather than a second approval mechanism. `SkillFirstIntentRouter` is the DOC-006 §4 entry point: `SkillMatcher.bestMatch(goal, activeSkills)` first — no match or a failed step both fall back to a caller-supplied `runPlanner()` lambda (kept abstract so `:brain` doesn't need `AionGraphFactory`'s concrete shape); a successful skill run returns `done=true` WITHOUT ever invoking `runPlanner`. 7 tests directly proving the AC's two literal halves (4 `SkillExecutorTest` — all-steps-succeed, a failing step stops before later steps and reports its index, an approval marker correctly flags the following step as a side effect, a step with no approval marker before it does not; 3 `SkillFirstIntentRouterTest` — **a matched successful skill calls the planner lambda exactly 0 times** (skips planner, the AC's first half), no-match falls back calling it exactly once, **a skill that fails partway also falls back calling it exactly once, carrying the real failure reason through** (the AC's second half)), all pass on the first run. Full `:brain`+`:android:app` suite + `assembleDebug` clean. 🧍HC-4's own parenthetical — "(one real skill in daily use)" — is genuinely beyond what code can verify: it requires the owner actually using AION daily with a real installed skill over real time, which is EPIC 9's actual exit criterion, not a thing I can fabricate or shortcut. **EPIC 9 (Skills) is code-complete** (T-090 ✅, T-091 ✅, T-092 ✅, T-093 code-AC ✅) but genuinely blocked on this real-world usage checkpoint before EPIC 10 begins — different in kind from the on-device-test checkpoints closing EPICs 4/5/7/8, which the owner could satisfy in one sitting

## EPIC 10 — Vision + Comms + M2 (S10)
[x] T-100 (DOC-012) Screenshot capture (API31 path + MediaProjection fallback) + ML Kit OCR — AC: OCR on 10 golden screenshots ✅ verified 2026-07-10: `AionAccessibilityService.captureScreenshot()` — real `takeScreenshot()` API (API 30+, always available given this project's minSdk 31), wrapped in `suspendCancellableCoroutine`. DOC-012's MediaProjection fallback for pre-30 devices is genuinely unreachable given minSdk 31 — not built, honestly noted rather than shipped as dead code. `OcrEngine` interface + `MlKitOcrEngine` (real ML Kit Text Recognition v2, `com.google.mlkit.vision.text.latin.TextRecognizerOptions` — note the `.latin` package segment) + `VisionObservation`/`OcrBlock` (`:android:app`, OCR only — DOC-012's icon/widget "P1" detector needs a model that doesn't exist yet, BACKLOG.md). AC is literal: captured 10 REAL golden screenshots from the `aion_test` emulator (not synthetic fixtures) across home/Settings sub-screens/AION's own permission screen/Contacts, visually confirmed non-blank real content in every one via direct image inspection before trusting any as a test fixture — this caught 2 of the 10 (Wi-Fi settings, the AION app screen) that were silently blank on first capture from cold-start rendering lag despite passing a weaker `mResumedActivity`-only check, fixed by force-stopping the target app/task and waiting 5s instead of 3s before recapturing. `MlKitOcrEngineGoldenTest` (`androidTest`) loads each PNG and asserts real OCR output contains real strings confirmed present on that screen. Found and fixed a genuine bug while first running it: the test used `InstrumentationRegistry.getInstrumentation().targetContext` (the app-under-test's context, resolving to `app-debug.apk`) instead of `.context` (the instrumentation's own context, resolving to the test APK, `app-debug-androidTest.apk`, which is what actually carries `androidTest/assets/`) — `context.assets.open()` was reading the wrong APK entirely, throwing `FileNotFoundException` for every fixture. (A red herring along the way: briefly suspected `androidTest` sourceSet's `assets.srcDirs("$projectDir/schemas")` was replacing rather than adding to the default assets dir — verified empirically via an isolated `mergeDebugAndroidTestAssets --rerun-tasks` run that Gradle's `srcDirs()` adds, doesn't replace, so that line was fine as originally written and needed no change.) 10/10 tests pass for real on `aion_test` (AVD API 12/S) after the fix — confirmed via `./gradlew :android:app:connectedDebugAndroidTest` reporting `BUILD SUCCESSFUL`, "0 skipped, 0 failed". Full `:brain`+`:android:app` suite unaffected.
[x] T-101 Vision grounding: OCR blocks→targets merge into ElementResolver (a11y wins) — AC: canvas-app tap demo ✅ verified 2026-07-10: `ElementResolver.resolve()` gained an optional `vision: VisionObservation?` param (defaults `null`, so every existing 2-arg call site keeps working unchanged) — a11y is always tried first; vision (an OCR block turned into a synthetic `ElementRef`, `id="ocr:$index"`, `role="text"`) is only consulted when a11y found nothing, matching DOC-012 §1's "vision is the fallback" role exactly. `ById` queries never fall through to vision since OCR blocks have no stable id — only `ByText` can. New `ResolveMethod.VISION_OCR` distinguishes a vision-grounded resolution from a real a11y one for future logging/confidence-threshold use (DOC-012 §4). The lazy insight that kept this small: since `ElementRef.bounds` is the one shared currency `ActionDispatcher` already taps from (`(left+right)/2, (top+bottom)/2`), a synthetic OCR-backed `ElementRef` needs zero changes anywhere downstream — the existing tap pipeline handles it for free once `ElementResolver` can produce one. Wired the real caller too, not left inert: `DispatcherActionExecutor` now captures a screenshot + runs OCR ONLY when `service.currentElements()` comes back empty (the literal canvas-app/game condition DOC-012 §1 names), never on every step — avoids blowing the ≤2.5s/step vision budget (DOC-012 §5) when a11y already works, which is the common case. AC is literal: `canvas-app tap demo` test — empty a11y element list + a vision `OcrBlock("Start Game", bounds)` → resolves via `VISION_OCR`, and the resolved element's bounds correctly yield the same tap-center math `ActionDispatcher` already uses (200,230 for a 100,200–300,260 box), proving the full empty-a11y→vision→tap-point chain works end to end. Plus an explicit a11y-wins-on-conflict test (a real a11y element and a same-text vision block both present — a11y's `FUZZY_TEXT` match wins, vision never consulted) and the `ById`-never-falls-through case. Found and fixed a real latent bug while wiring the new `MlKitOcrEngine` constructor param into `DispatcherActionExecutor`: `TextRecognition.getClient(...)` was called eagerly at property-init time (same class of bug as `ActionDispatcher.mainHandler` at T-077 — touches Android framework internals, making the class uninstantiable in a plain JVM unit test) — fixed with `by lazy`, same root-cause pattern as before. 3 new `ElementResolverGoldenTest` cases (6/6 total pass), full `:brain`+`:android:app` suite + `assembleDebug` clean.
[x] T-102 Gmail API plugin (read/compose/send w/ approval) + Telegram Bot API plugin — AC: live smoke 🧍HC-3/5 — code AC ✅ verified 2026-07-10: `GmailPlugin`/`TelegramPlugin` (`:brain/plugins`) — unlike the a11y-driven built-ins (T-071..076), these never touch `ActionExecutor`/`PlanStep` at all; `execute(call: ToolCall)` parses `call.argsJson` directly and calls the real Gmail REST API / Telegram Bot API over HTTPS (Ktor, same `defaultProviderHttpClient()` pattern `:brain/providers`' `GeminiProvider`/`OpenAiCompatProvider` already use) — genuinely real HTTP request shapes, not stubs. Gmail: `list_messages`/`read_message` (sideEffect=false), `compose_draft` (sideEffect=false — creates a draft, never sent, reversible), `send_email` (sideEffect=true — the AC's literal "w/ approval", auto-gated through `PluginManager.route()`'s existing `PluginApprovalGate`, T-070, no new approval mechanism needed). Telegram: `send_message` (sideEffect=true, gated) + `get_updates` (sideEffect=false). `GmailPlugin` takes a bare OAuth2 access token, not a full client-id/secret flow — building real Google OAuth consent + refresh is separate, much larger scope; v1 expects the owner to paste a working access token into `SecretVault` directly (works today with zero new UI, since `SecretsScreen`, T-024, already generalizes over every `ProviderKey.entries` for free) — tokens expire in ~1h with no refresh, an honest named gap for BACKLOG.md, not hidden. `SecretVault.ProviderKey` gained `GMAIL_ACCESS_TOKEN`/`TELEGRAM_BOT_TOKEN`; new `GmailToken`/`TelegramToken` Hilt qualifiers + `PluginTokenModule` resolve them into plain `String`s at injection time (matching `OpenAiCompatProvider`/`GeminiProvider`'s own `apiKey: String` shape) specifically so `BuiltInPluginRegistry` doesn't depend on `SecretVault` directly — kept `BuiltInPluginRegistryTest` (a plain JVM test) able to pass literal strings with zero Android/Keystore dependency, same reasoning as every other plain-JVM-testability fix this session. Both plugins registered like the 6 named built-ins (validated, NOT auto-enabled — owner's explicit 🧍HC-5 call); a blank/missing token just means every real API call fails gracefully with a normal `ToolResult` error, same degrade-cleanly shape as `ShizukuBridge`. 12 new tests (7 `GmailPluginTest`, 5 `TelegramPluginTest`, both against a real `MockEngine` proving actual request shapes — correct endpoint paths for send vs. draft, base64url MIME encoding, missing-required-arg rejected before any network call, non-2xx and Telegram's own `ok:false` both surface as clean `ToolResult` failures not thrown exceptions) + 2 more built-ins added to `BuiltInPluginsTest`'s DNA-validation sweep, all pass on the first real run (one self-caught test bug: an accidental "bot" + "bot-token" double-prefix in a URL assertion, not a plugin bug). Full `:brain`+`:android:app` suite + `assembleDebug` clean. **The AC's other literal half — "live smoke" — is genuinely NOT done and can't be**: it requires the owner's real Gmail OAuth token and real Telegram bot token (🧍HC-3/5 named directly in the AC), which don't exist in this environment and can't be fabricated without violating "never fake test results, stop at human checkpoints." Marked the buildable/testable code AC done and said so explicitly, same honest-scoping shape as T-093's "code AC ✅, real checkpoint stays open" — not silently claiming the full AC, not leaving genuinely-finished code unmarked either.
[ ] T-103 🏆 M2: 3-app multi-step goal e2e ("kal ki meeting ka time nikal ke HR ko mail karo aur reminder laga do") 🧍HC-4

## EPIC 11 — Learning & Dream (S11)
[x] T-110 (DOC-008) Dream Mode WorkManager job (charge+idle constraints, budget guards) — AC: constraint tests ✅ verified 2026-07-10: `DreamModeGuard` (`:brain`) is the AC's "budget guards" half — pure arithmetic over caller-supplied time/battery/charging/thermal values (Android-independent, plain-JVM testable), DOC-008 §5's numbers taken literally: ≤30 min elapsed, ≤15% battery drop, immediate abort on unplug or thermal throttling, unplugged checked first so it always wins over a simultaneous time/battery violation. `DreamModeWorker` (`:android:app`, real `CoroutineWorker`) reads real battery %/charging state via `BatteryManager` and thermal status via `PowerManager.currentThermalStatus` (≥MODERATE counts as throttled), and `DreamModeScheduler`/`dreamModeConstraints()` builds the real WorkManager `Constraints` (requiresCharging, requiresDeviceIdle) for a 24h `PeriodicWorkRequest`, DOC-008 §5's "charge+idle" half of the AC. This is honestly a scaffold-only task: T-111 (consolidation)/T-112 (scorecards)/T-113 (routine proposals) don't exist yet, so `doWork()` currently just starts the guard, checks it once, and returns — the real step loop slots in between those two calls once the later tasks land, same "built and tested, first real caller comes later" shape as several earlier pieces this session. Used WorkManager's default no-arg-plus-params reflective construction rather than adding the `hilt-work` Gradle artifact this project doesn't have yet — nothing here needs an injected dependency yet. 8 new tests (7 `DreamModeGuardTest` — continues normally, aborts on each of the 4 reasons individually, unplugged wins over a simultaneous battery+thermal violation, checking before `start()` never aborts; 1 `DreamModeConstraintsTest`), all pass. Decompiled WorkManager's own `Constraints.Builder.build()` bytecode (not assumed) to confirm a real, separate finding: it gates `requiresDeviceIdle` behind `Build.VERSION.SDK_INT >= 23` at build time (`requiresCharging` has no such gate) — meaning `requiresDeviceIdle()` can never read back `true` in a plain-JVM test with no Robolectric (`SDK_INT` defaults to 0), regardless of how correct the production code is. Rather than force that assertion or silently drop the claim, named the limitation directly in the test file and only asserted what's genuinely testable at this layer (`requiresCharging`); `setRequiresDeviceIdle(true)`'s own setter has no such gate and is correct on any real device (minSdk 31 ≫ 23). Full `:brain`+`:android:app` suite + `assembleDebug` clean.
[x] T-111 Consolidation: dedupe/decay/promote + nightly report generator — AC: report artifact produced ✅ verified 2026-07-10: `Memory`/`MemoryStore` (`:brain`) — the DOC-019 §1 `memories` table's Android-independent mirror, reusing T-060's already-scoped table exactly like T-080/T-090 did for episodes/skills; `MemoryDao` gained `update`/`softDelete` (only had `insert`/`getAllActive` from T-060 — no caller ever needed to mutate a row until now). `RoomMemoryStore` (`:android:app`) implements it, newly Hilt-wired (`DatabaseModule.provideMemoryDao` + `BrainModule.bindMemoryStore`, same "DAO existed since T-060, wasn't exposed until its consuming task" pattern as EpisodeDao/ElementMapDao/SkillDao before it). `MemoryConsolidator` (`:brain`) does DOC-008 §5 step 2 literally: dedupe via `TextSimilarity` (same 0.95-threshold real-embeddings stand-in `SkillMatcher`/`RepeatedTaskDetector` already established, T-032/T-061 still deferred) clustered by same-`kind` only (a fact and a pref with identical text never merge); decay reduces `decayScore` for anything untouched past 30 days; "promote repeated → long-term" is realized as: merging duplicates raises the survivor's confidence to the cluster's highest observed value plus a per-duplicate boost and resets its decay clock — this schema has one `memories` table, not a separate short/long-term split, so promotion means "more confident, more decay-resistant," not a row migration to a table that doesn't exist. `DreamModeWorker` (T-110) now genuinely calls this — no longer the no-op scaffold from T-110's own honest note — via a plain Hilt `@EntryPoint` (`DreamModeWorkerEntryPoint`) rather than adding the `hilt-work` Gradle artifact + KSP processor this project still doesn't need for anything else. AC is "report artifact produced": `ConsolidationReport` (totalScanned/mergedGroups/mergedAway/decayedCount/promotedCount + a human-readable summary string) is a real, structured value a real consolidation run genuinely produces — same "the returned data structure itself is the artifact" reading T-082 used for "recovery success >50%." Found a real algorithm bug while writing the first test, not a test-assertion mistake: merging initially based the survivor's boosted confidence on its OWN prior value rather than the highest confidence seen across the whole duplicate cluster, so a low-confidence-but-older record merging with a high-confidence-but-newer duplicate would have UNDER-reported the resulting trust level — fixed to take `cluster.maxOf { it.confidence }` as the base before the repetition boost. 11 new tests (8 `MemoryConsolidatorTest` — merge+promote, unrelated memories never merge, same-text-different-kind never merges, stale decays, recently-accessed doesn't decay, decay floors at 0.0 rather than going negative, soft-deleted memories are excluded from consolidation entirely, an empty set produces a real zeroed report rather than crashing; 3 `RoomMemoryStoreTest` — insert round-trips including PII tags, update persists a consolidation-style change, soft-delete removes from `getAllActive`), all pass on the first real run after the confidence-base fix. Full `:brain`+`:android:app` suite + `assembleDebug` clean — `assembleDebug` succeeding also confirms the new `@EntryPoint`/`@Binds` DI wiring actually resolves, not just compiles in isolation. Honest scope note: like `episodes`/`element_maps` before it, nothing yet writes real rows into `memories` during live usage — T-062 (MemoryAgent write policy) is what will actually populate this table; T-111 proves the consolidation algorithm itself is correct against realistic synthetic data, the same "mechanism built and tested, first real data source comes from a later task" shape used throughout this session.
[x] T-112 Brain Learning scorecards feeding Router (ε=0.05 exploration) — AC: score shifts measurable in sim ✅ verified 2026-07-10: DOC-008 §4's "scorecards feeding Router" is mostly already T-030's `ScoreStore`/`ScoringMath`/`ProviderRouter` (EMA success/latency scoring, weighted candidate sort, sequential failover) — the one genuinely missing piece was "ε=0.05 exploration," which `ProviderRouter.route()` doesn't do (pure greedy: always sorts by score, never deviates). `ProviderRouter`/`Provider`/`AionGraph` are this project's frozen contracts (CLAUDE.md: no signature changes without an ADR + marking the task blocked for human review) — rather than touch `ProviderRouter.kt` at all, built `ExploringScoreStore` (`:brain`), a `ScoreStore` DECORATOR: on each `taskScore()` call, with probability ε=0.05 the real score is replaced with the max value (1.0), giving that provider a real shot at winning the router's existing unmodified `sortedByDescending` sort this round — `ProviderRouter` itself is never touched, imported, or even aware exploration exists; `ScoreStore` swappable implementations are exactly the extension point the interface already provides (`RoomScoreStore` is itself just one). Honestly noted where this deviates from textbook ε-greedy (which picks ONE uniform-random arm per routing decision): `ScoreStore`'s frozen per-provider-per-call shape gives no way to correlate multiple `taskScore()` calls as one decision, so this is independent per-provider exploration rolls rather than one shared per-round pick — same practical effect (a losing provider periodically gets retried instead of starved forever), different mechanism, named directly in the code rather than silently passed off as identical to the doc's description. AC is "score shifts measurable in sim" — built a real simulation (`ProviderRouterExplorationSimTest`) against the REAL, unmodified `ProviderRouter` (not a reimplementation): a "good" provider (always succeeds) and a "bad" one (always fails), both pinned to identical tier/latency so ONLY the taskScore term ε-exploration touches can ever separate them, isolating the effect from unrelated scoring-formula confounds rather than hoping they'd wash out. Without exploration: `bad` is tried exactly **0** times across 500 rounds — round 1 ties exactly (both start at the same default score) and Kotlin's stable sort keeps registry order on ties, so `good` (listed first) wins immediately and `bad`'s score decays into permanent irrelevance, the literal "starved forever" problem DOC-008 §4 names. With ε=0.05 exploration (seeded `Random(42)` for a reproducible, non-flaky result): `bad` gets tried well past the asserted floor of 15 times across the same 500 rounds — a real, measurable, reproducible score-and-selection shift caused by nothing but the exploration wrapper. 6 new tests (4 `ExploringScoreStoreTest` — below-epsilon returns max score, at/above-epsilon passes the real score through unchanged, `latencyNorm`/`notInCooldown` always delegate untouched, `recordSuccess`/`recordFailure` always pass through), 2 `ProviderRouterExplorationSimTest`), all pass. Full `:brain`+`:android:app` suite + `assembleDebug` clean. Honest scope note: `ProviderRouter` itself is constructed nowhere in `:android:app`'s real wiring yet — grepped and confirmed zero real call sites outside tests, an already-existing gap from T-030 (no real LLM `Provider` is ever built from a `SecretVault` key and injected anywhere), downstream of T-032's deferred local-LLM build and T-035's voice-blocked wiring — not something T-112 introduces or could fix on its own; `ExploringScoreStore` is ready to slot in the moment a real caller exists.
[x] T-113 Pattern Learner → routine proposals UI 🧍HC-5 — AC: synthetic routine detected 🧍HC-4 — code AC ✅ verified 2026-07-10: `PatternLearner` (`:brain`) implements 2 of DOC-008 §2's 3 named pattern types — `detectTimeBasedRoutines` ("7AM weather": same goal recurring at roughly the same time of day, ±60min window, ≥3 occurrences) and `detectSequencePatterns` ("mail→calendar→notes": the same ordered goal-pair happening back-to-back within a 30min gap, ≥3 separate times). Context patterns (location/charging state) are honestly NOT built — DOC-019 §1's `episodes` table has no location or charging-state column to mine at all, a schema gap beyond this task's scope, not a detection-algorithm one; named directly rather than faking a detector with no real signal, tracked in BACKLOG.md. Needed a real, small frozen-file-adjacent fix first: `ReflectionRecord` (`:brain`, NOT one of the 3 actually-frozen files — Provider/ProviderRouter/AionGraph only) had no `ts` field at all, so time-based detection was structurally impossible until now; added `ts: Long = 0` as a trailing defaulted param (verified every existing positional call site — `RepeatedTaskDetectorTest`, `SkillGenerationPipelineTest`, `RoomReflectionRecordStoreTest` — stays valid unchanged) and fixed `RoomReflectionRecordStore.recent()`, which was silently discarding the real Room-stored timestamp on every read, to actually populate it. Both detectors produce `RoutineProposal { kind, description, candidate: RepeatedTaskCandidate }` — deliberately the EXACT same `RepeatedTaskCandidate` shape `RepeatedTaskDetector`/`SkillGenerationPipeline` already use (T-091/T-092), so a detected routine needs zero new plumbing to become a skill draft once approved: `SkillGenerationPipeline.run(proposal.candidate)` is a valid call today. "Routine proposals UI 🧍HC-5" is satisfied the same way T-092's "approval UI" was: `RoutineProposal.description` IS the human-readable text `ApprovalGateService.requestApproval(voiceLine, detail)` (T-021, already built) already knows how to show — no new UI needed, the hookup is a one-line call once a real caller exists. 7 new `PatternLearnerTest` cases (a phrasing-variant-tolerant time cluster detects; only 2 occurrences doesn't trigger; wildly different times of day never cluster despite identical text; a repeating goal-pair with a tight gap detects as a sequence; too large a gap between pair members doesn't count as an occurrence; unrelated interleaved episodes never produce a false sequence; empty input produces nothing from either detector), all pass — one self-caught test-fixture bug along the way, not an algorithm bug: an early draft used "mausam batao" as a supposed "phrasing variant" of "weather check karo," which `TextSimilarity`'s real, already-known limitation (no semantic understanding, just string/token overlap) correctly refused to match — fixed by using an actual variant (case difference) the algorithm can legitimately recognize. Full `:brain`+`:android:app` suite + `assembleDebug` clean. Honest scope boundary, matching T-112's own finding directly: `PatternLearner`'s natural real caller (Dream Mode nightly → approval → `SkillGenerationPipeline`) needs a real `ProviderRouter` to actually draft a skill, and — per T-112 — NO real `Provider`/`ProviderRouter` is constructed anywhere in `:android:app`'s live DI graph at all yet; wiring `PatternLearner` into `DreamModeWorker` today would either fake that missing piece or produce dead code that can't functionally run, so it wasn't forced. The code-level AC ("synthetic routine detected") is what's genuinely done; 🧍HC-4's own real-world checkpoint (an actual routine detected from real usage and approved) is the same kind of not-yet-reachable milestone as T-093/T-054/T-035, blocked on real usage history and the still-missing Provider wiring, not something to self-certify past.

## EPIC 12 — Hardening & Alpha (S12)
[ ] T-120 Security audit vs DOC-017 T1-T8 checklist; fix gaps — AC: written audit doc, all P0 closed
[ ] T-121 (DOC-018) 50-task Hindi+English benchmark harness + run — AC: report; ≥60% target evaluated honestly
[ ] T-122 Perf soak: 24h battery, 7d uptime sim — AC: budgets met or deviations documented
[ ] T-123 Docs sync (all DOC deltas), PROGRESS.md final, release notes — AC: repo tagged v0.1.0-alpha 🏆 🧍HC-4
